<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tamer.raed.doctorappointment">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_health_insurance"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_health_insurance"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoctorAppointment">
        <activity android:name=".patient.ui.BookAppointmentActivity" />
        <activity android:name=".patient.ui.DoctorDetailsActivity" />
        <activity android:name=".patient.ui.patientDashboard.SearchActivity" />
        <activity android:name=".patient.ui.PatientSignUpActivity" />
        <activity android:name=".patient.ui.patientDashboard.PatientDashboardActivity" />
        <activity android:name=".doctor.ui.DoctorSignUpActivity" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/welcome" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>